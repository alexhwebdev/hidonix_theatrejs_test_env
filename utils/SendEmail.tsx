import { IonFormData, FormData } from "@/types/pageContent.types";

export function sendEmail(data: FormData) {
  // TODO: send email
  console.log('data ', data);
}

export function sendIonForm(data: IonFormData) {
  // TODO: send email
  console.log('data ', data);
}